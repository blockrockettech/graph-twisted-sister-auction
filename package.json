{
  "name": "twisted-sister-auction",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ blockrockettech/twisted-sister-auction",
    "create-local": "graph create --node http://localhost:8020/ blockrockettech/twisted-sister-auction",
    "remove-local": "graph remove --node http://localhost:8020/ blockrockettech/twisted-sister-auction",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 blockrockettech/twisted-sister-auction"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.2",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
